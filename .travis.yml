language: node_js
before_script:
  - npm install -g bower
  - bower install
  - npm run build
node_js: '7'
addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
script:
  - xvfb-run npm run test
dist: trusty
sudo: false
